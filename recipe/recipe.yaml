context:
  version: "0.25.4"

package:
  name: agent-browser
  version: ${{ version }}

source:
  url: https://github.com/vercel-labs/agent-browser/archive/refs/tags/v${{ version }}.tar.gz
  sha256: 099e03303a70517a8f8480f42a10d5add345b2e572089a5ca0e80f20a77058d3

build:
  number: 0
  script:
    env:
      CARGO_PROFILE_RELEASE_STRIP: symbols
      CARGO_PROFILE_RELEASE_LTO: fat
    content:
      - cd ./cli
      - if: unix
        then:
          - cargo auditable install --no-track --locked --root $PREFIX --path .
        else:
          - cargo auditable install --no-track --locked --root %LIBRARY_PREFIX% --path .
      - cargo-bundle-licenses --format yaml --output ../THIRDPARTY.yml

requirements:
  build:
    - ${{ compiler('rust') }}
    - ${{ stdlib('c') }}
    - cargo-bundle-licenses
    - cargo-auditable

tests:
  - script:
      - agent-browser --version
  - package_contents:
      bin:
        - agent-browser
      strict: true

about:
  homepage: https://github.com/vercel-labs/agent-browser
  summary: Browser automation CLI for AI agents
  description: |
    Browser automation CLI for AI agents. Fast native Rust CLI.
  license: Apache-2.0
  license_file:
    - LICENSE
    - THIRDPARTY.yml
  documentation: https://agent-browser.dev
  repository: https://github.com/vercel-labs/agent-browser

extra:
  recipe-maintainers:
    - yuikisaito
